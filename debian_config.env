# TailSentry Debian Configuration
# Place this file at /etc/tailsentry/config.env

# Tailscale binary paths - uncomment and adjust if needed
# TAILSCALE_PATH=/usr/sbin/tailscale
# TAILSCALED_PATH=/usr/sbin/tailscaled

# Log settings
LOG_LEVEL=INFO
LOG_FORMAT="%(asctime)s - %(levelname)s - %(name)s - %(message)s"

# Data directory
TAILSENTRY_DATA_DIR=/opt/tailsentry/data

# Security settings
# Generate a strong random secret: python -c "import secrets; print(secrets.token_hex(32))"
SESSION_SECRET=replace_with_random_hex_string_for_session_security

# Development mode (set to "false" in production)
DEVELOPMENT=false

# Metrics collection
METRICS_ENABLED=false

# API access - uncomment and set if using Tailscale API with PAT
# TAILSCALE_PAT=your_personal_access_token
# TAILSCALE_TAILNET=your_tailnet
